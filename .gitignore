target/
*.class

# IntelliJ — keep run configurations, ignore everything else
.idea/*
!.idea/runConfigurations/

# AI handover file — kept locally only, not published to GitHub
CLAUDE.md

# Gradle
.gradle
build/

# Local scratch / review CSVs (kept locally, not published)
*.csv

# Internal design / handover docs — kept locally, not published to GitHub
docs/CARD-STATS.md
docs/multi-account-design.md
docs/PLUGIN-HUB-SUBMISSION.md
